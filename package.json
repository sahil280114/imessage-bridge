{
  "name": "amber-imessage-bridge",
  "version": "0.1.0",
  "description": "Bridges Amber backend to iMessage via openclaw/imsg on a local Mac.",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
